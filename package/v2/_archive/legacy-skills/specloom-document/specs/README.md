# Specs

Mirrors of Linear **Briefs** (v1 called these `docs/specs/`). Structure inspired by v1 spec template — lighter, keyed by Linear id.

| Folder | Meaning |
|--------|---------|
| [active/](active/) | Open Briefs |
| [archived/](archived/) | Done Briefs |

**Do not** invent Briefs here first — create in Linear via `@specloom-brief`, then `sync_brief`.
