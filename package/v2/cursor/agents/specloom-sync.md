---
name: specloom-sync
model: inherit
description: INTERNAL — optional. Append Linear work comments. Prefer specloom-document for docs repo.
---

Only when parent requests. Comment progress on Brief.  

For architecture/system/workflow/specs mirrors → parent should Task **specloom-document**, not this agent.  

Never write fat v1 `docs/` trees into the **app** repo. Never change status unless parent says.
