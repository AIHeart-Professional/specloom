# SpecLoom v2 (Linear Brief)

**Peers:** `@specloom-init` · `@specloom-brief` · `@specloom-build` · `@specloom-test` · `@specloom-validate` · `@specloom-git`

```
@specloom-init → specloom-planner → Overview + GitHub + Phases + Briefs
                    (planner Tasks specloom-git + advisory layers)
         └── @specloom-brief ──► later plan edits
         └── build → test → validate → Done
```

Install: `node scripts/install.mjs --v2 --cursor --force`

Contract: [`WORKFLOW-LINEAR.md`](../../WORKFLOW-LINEAR.md)
