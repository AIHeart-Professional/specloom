# SpecLoom v1 (legacy)

In-app `docs/` planning: ideas → features → specs. Sign-off + `active_work.json`.

**Peers:** `@specloom-work-creator` · `@specloom-implement` · `@specloom-tester` · `@specloom-validator` · `@specloom-git`

Install: `node scripts/install.mjs --v1` (default).

Live contract: repo root [`WORKFLOW.md`](../../WORKFLOW.md).

**Do not mix with v2 agents in the same install.**