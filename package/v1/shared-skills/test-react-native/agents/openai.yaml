interface:
  display_name: "Test React Native"
  short_description: "Internal application-development workflow skill for test-react-native."
  default_prompt: "Use $test-react-native only from its assigned SDD custom agent."
policy:
  allow_implicit_invocation: false
