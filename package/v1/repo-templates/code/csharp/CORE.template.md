# C# — Project Extensions

Universal C# standards live in user-level **code-csharp** skill (`~/.cursor/skills/` or `~/.agents/skills/`).

Add **project-specific** rules here only — do not duplicate Microsoft conventions.

## Examples (delete or replace)

- Target framework: `net8.0`
- Analyzer rule suppressions with justification
- Shared `Directory.Build.props` conventions
- Logging library and correlation ID pattern
- Nullable migration boundaries

List this file in spec **Required Context** when game tasks touch C# outside universal rules.
