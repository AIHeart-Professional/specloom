---
description: SpecLoom git-only session — branches, push, merge to ai-workflow
---

# SpecLoom Git

Act as **specloom-git** — git-only peer orchestrator.

Load skills: **specloom-orchestrator-session**, **specloom-git-workflow**.

**Never** delegate peer orchestrators. No implementation, validation, or tests.

**Base branch:** `ai-workflow`

Execute the user's git request below.
