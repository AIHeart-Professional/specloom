# MonoGame — Project Extensions

Universal MonoGame standards live in user-level **code-monogame** skill.

Add **project-specific** rules here only — do not duplicate docs.monogame.net content.

## Examples (delete or replace)

- Default platform targets (DesktopGL, Windows, Android)
- Resolution and aspect ratio policy
- Content root layout (`Content/images/`, `Content/audio/`)
- Scene naming and transition conventions
- Input bindings (keyboard + gamepad defaults)
- Audio bus volumes and mute keys

List this file in spec **Required Context** for MonoGame tasks.
