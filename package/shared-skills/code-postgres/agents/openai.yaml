interface:
  display_name: "Code Postgres"
  short_description: "Internal application-development workflow skill for code-postgres."
  default_prompt: "Use $code-postgres only from its assigned SDD custom agent."
policy:
  allow_implicit_invocation: false
