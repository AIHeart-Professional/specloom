interface:
  display_name: "Code React Native"
  short_description: "Internal application-development workflow skill for code-react-native."
  default_prompt: "Use $code-react-native only from its assigned SDD custom agent."
policy:
  allow_implicit_invocation: false
