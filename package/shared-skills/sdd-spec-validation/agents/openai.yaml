interface:
  display_name: "SDD Spec Validation"
  short_description: "Internal application-development workflow skill for sdd-spec-validation."
  default_prompt: "Use $sdd-spec-validation only from its assigned SDD custom agent."
policy:
  allow_implicit_invocation: false
