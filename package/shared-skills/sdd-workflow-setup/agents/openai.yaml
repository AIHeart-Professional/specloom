interface:
  display_name: "SDD Workflow Setup"
  short_description: "Internal application-development workflow skill for sdd-workflow-setup."
  default_prompt: "Use $sdd-workflow-setup only from its assigned SDD custom agent."
policy:
  allow_implicit_invocation: false
