interface:
  display_name: "SDD Create Spec"
  short_description: "Internal application-development workflow skill for sdd-create-spec."
  default_prompt: "Use $sdd-create-spec only from its assigned SDD custom agent."
policy:
  allow_implicit_invocation: false
