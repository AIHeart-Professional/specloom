interface:
  display_name: "SDD Test Validation"
  short_description: "Internal application-development workflow skill for sdd-test-validation."
  default_prompt: "Use $sdd-test-validation only from its assigned SDD custom agent."
policy:
  allow_implicit_invocation: false
