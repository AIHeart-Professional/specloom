interface:
  display_name: "SDD Orchestrator Protocol"
  short_description: "Internal application-development workflow skill for sdd-orchestrator-protocol."
  default_prompt: "Use $sdd-orchestrator-protocol only from its assigned SDD custom agent."
policy:
  allow_implicit_invocation: false
