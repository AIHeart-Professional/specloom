interface:
  display_name: "Test Monogame"
  short_description: "Internal application-development workflow skill for test-monogame."
  default_prompt: "Use $test-monogame only from its assigned SDD custom agent."
policy:
  allow_implicit_invocation: false
