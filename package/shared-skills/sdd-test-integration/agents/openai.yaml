interface:
  display_name: "SDD Test Integration"
  short_description: "Internal application-development workflow skill for sdd-test-integration."
  default_prompt: "Use $sdd-test-integration only from its assigned SDD custom agent."
policy:
  allow_implicit_invocation: false
